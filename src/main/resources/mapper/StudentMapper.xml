<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xmu.cms.dao.StudentDao">

    <select id="getAllStudents" resultType="com.xmu.cms.entity.Student">
        SELECT student_id       as studentId,
               account,
               password,
               email,
               name,
               phone,
               sex,
               message_interval as messageInterval,
               activation
        FROM student
    </select>

    <insert id="newStudent" parameterType="com.xmu.cms.entity.Student">
        INSERT INTO student (student_id, account, password, email, name, message_interval, activation)
        VALUE (#{studentId}, #{account}, #{password}, #{email}, #{name}, #{messageInterval}, #{activation})
    </insert>

    <update id="modifyStudentById">
        UPDATE student
        SET name    = #{name},
            account = #{account},
            email   = #{email}
        WHERE student_id = #{studentId}
    </update>

    <update id="modifyStudentPasswordById">
        UPDATE student
        SET password = #{password}
        WHERE student_id = #{studentId}
    </update>

    <delete id="deleteStudentById" parameterType="java.lang.Integer">
        DELETE
        FROM student
        WHERE student_id = #{studentId}
    </delete>

    <select id="getPasswordByAccount" parameterType="java.lang.String">
        SELECT password
        FROM student
        WHERE account = #{account}
    </select>
</mapper>