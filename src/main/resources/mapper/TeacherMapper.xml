<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xmu.cms.mapper.TeacherMapper">

    <select id="getAllTeachers" resultType="com.xmu.cms.entity.Teacher">
        SELECT teacher.id           as teacherId,
               teacher.teacher_num  as account,
               teacher.password,
               teacher.email,
               teacher.teacher_name as teacherName,
               teacher.phone,
               teacher.msg_interval as messageInterval,
               teacher.is_activated as activation
        FROM teacher
    </select>

    <update id="updateTeacher">
        UPDATE teacher
        SET teacher_num  = #{teacher.account},
            password     = #{teacher.password},
            email        = #{teacher.email},
            teacher_name = #{teacher.courseName},
            phone        = #{teacher.phone},
            msg_interval = #{teacher.messageInterval},
            is_activated = #{teacher.activation}
        WHERE id = #{teacher.teacherId}
    </update>

    <insert id="insertTeacher">
        INSERT INTO teacher (teacher_num, password, email, teacher_name, phone, msg_interval, is_activated)
        VALUE (#{teacher.account},
               #{teacher.password},
               #{teacher.email},
               #{teacher.courseName},
               #{teacher.phone},
               #{teacher.messageInterval},
               #{teacher.activation})
    </insert>

    <delete id="deleteTeacher" parameterType="java.lang.Integer">
        delete
        FROM teacher
        WHERE id = #{teacherId}
    </delete>

    <select id="getTeacherByAccount" resultType="com.xmu.cms.entity.Teacher">
        SELECT id           as teacherId,
               teacher_num  as account,
               password,
               email,
               teacher_name,
               phone,
               msg_interval as messageInterval,
               is_activated as activation
        FROM teacher
        WHERE teacher_num = #{account}
    </select>

    <select id="getTeacherByName" resultType="com.xmu.cms.entity.Teacher">
        SELECT id           as teacherId,
               teacher_num,
               password,
               email,
               teacher_name,
               phone,
               msg_interval as messageInterval,
               is_activated
        FROM teacher
        WHERE teacher_name = #{name}
    </select>

    <select id="getTeacherById" resultType="com.xmu.cms.entity.Teacher">
        SELECT id           as teacherId,
               teacher_num,
               password,
               email,
               teacher_name,
               phone,
               msg_interval as messageInterval,
               is_activated
        FROM teacher
        WHERE id = #{teacherId}
    </select>

    <select id="getIdByAccount" resultType="java.lang.Integer">
        SELECT id as teacherId
        FROM teacher
        WHERE teacher_num = #{account}
    </select>

    <select id="getEmailByAccount" resultType="java.lang.String">
        select email
        from teacher
        where teacher_num = #{account}
    </select>
</mapper>